<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- <link rel="stylesheet" href="/stylesheets/style.css"> -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md px-5 py-2 bg-zinc-800" type="text" placeholder="Type Here" name="title">
                <textarea class="block w-full rounded-md px-5 py-2 mt-2 resize-none bg-zinc-800" placeholder="Write your task detail here" name="details"></textarea>
                <input class="mt-2 px-2 py-2 rounded-md bg-blue-600 text-bold" type="Submit" value="Create Task">
            </form>
        </div>
     <div class="task flex gap-3 flex-wrap p-10">
        <% if(files.length > 0 ) { %>
        <% files.forEach(function(val){ %>
        <div class="task w-73  px-3 py-4 rounded-md bg-zinc-800">
            <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
           <div class=" flex w-full text-full justify-between item-center mt-3">
            <a class="text-blue-400 inline-block " href="/files/<%= val %>">Read more.</a>
            <a class="text-zinc-600 ml-3" href="/edit/<%= val %>">edit name</a>
           </div>
        </div>
    <%  }) %>
    <% } else { %>
        <h3 class="text-zinc-600">No task yet.</h3>
    <% } %>
    </div>
    </div>

</body>
</html>